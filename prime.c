#include<stdio.h>
int main()
{
    int i,n;
    printf("enter a number");
    scanf("%d",&n);
    if
        (n==0)||(n==1)
    {
        printf("%d", is not prime,n);
    }
    else
        {
        for(i=2;i<=n,i++)
        {
            if (n%i==0)
            {
                break;
            }
        }
        if (n==i)
        {
            printf("%d", is prime,n);
        else
        {
            printf("%d", is not prime,n);
        }
    }
}